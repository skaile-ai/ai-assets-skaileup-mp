# Worked examples

Two skills from the old collection, ported into the `-mp` shape, kept as reference for
what the template means in practice:

| Example | Before | After |
|---|---|---|
| `concept-brief/` | 289 lines | **80** |
| `mockup-walkthrough-astro/` | 1,133 lines | **110** |

[`WHY.md`](./WHY.md) records what came out and why — including that the astro skill's bulk
was duplication of `contracts/walkthrough_renderer.md` rather than length.

These are **examples, not live skills**: their final names and boundaries belong to the
domain ports. Nothing installs from `docs/`.
