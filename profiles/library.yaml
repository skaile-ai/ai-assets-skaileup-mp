# Profile — library
#
# What a project of this type grows, and what it never will. `concept-scope` resolves
# `project_type` against this file; a skill that finds an artifact listed under `not_grown`
# missing has found a project shaped this way, not a gap to report.

profile: library
description: "Library package — an API surface instead of features, no UI and nothing deployed"
version: 2

not_grown:
  - journeys
  - screens
  - brand-identity
  - brand-tokens
  - behaviors
  - datamodel
  - mockup
  - mockup-storybook
